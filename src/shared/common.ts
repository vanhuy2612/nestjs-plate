import { Env } from "./env";

export const THROTTLE_TTL = Env.THROTTLE_TTL;
export const THROTTLE_LIMIT = Env.THROTTLE_LIMIT;
export const OTP_EXPIRE_IN = 5 * 60; // 5 minutes
export const PAGE_DEFAULT = 1;
export const PAGE_SIZE_DEFAULT = 20;
